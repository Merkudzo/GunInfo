<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".GunListActivity">

    <include android:id="@+id/toolbar_gun"
        layout="@layout/toolbar"
        />

    <fragment
        android:id="@+id/gun_fragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="3dp"
        android:layout_below="@+id/toolbar_gun"
        android:name="com.merkudzo.gunsinformations.GunListLayout"
        tools:layout="@layout/gun_list_layout" />

</RelativeLayout>